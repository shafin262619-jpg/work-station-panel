{
  "name": "work-station-panel-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Work Station Panel backend API (Express + SQLite)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "node-cron": "^4.6.0"
  }
}
